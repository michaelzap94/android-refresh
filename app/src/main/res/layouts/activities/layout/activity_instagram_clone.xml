<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/instaRelLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".InstagramClone"
    >

    <ImageView
        android:id="@+id/instaImageView"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_marginTop="90dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        app:srcCompat="@drawable/instagram" />

    <EditText
        android:id="@+id/instaUsernameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/instaImageView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="13dp"
        android:ems="10"
        android:hint="Username"
        android:inputType="textPersonName" />

    <EditText
        android:id="@+id/instaPasswordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/instaUsernameEditText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="18dp"
        android:ems="10"
        android:hint="Password"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/instaSignUpButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/instaPasswordEditText"
        android:layout_alignStart="@+id/instaImageView"
        android:layout_marginTop="38dp"
        android:onClick="signUpOrLogIn"
        android:text="Sign Up" />

    <TextView
        android:id="@+id/instaLoginTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/instaSignUpButton"
        android:layout_alignBottom="@+id/instaSignUpButton"
        android:layout_marginStart="12dp"
        android:layout_toEndOf="@+id/instaSignUpButton"
        android:text="or, Login"
        android:textColor="@android:color/holo_blue_dark"
        android:textSize="20sp" />
</RelativeLayout>